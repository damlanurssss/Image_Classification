# Image_Classification
Image Classification using CNN
This project is a simple image classification model built using TensorFlow and Keras.
The goal is to train a neural network to correctly classify images into predefined categories.
📁 Project Overview
The project includes:
Loading and normalizing image data
Building a Sequential CNN model
Training and validating the model
Evaluating test accuracy
